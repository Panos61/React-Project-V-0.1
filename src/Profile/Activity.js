import React from 'react';
import AuthEvents from '../test/AuthEvents';

const ProfileActivity = () => {
  return (
    <div>
      {/* <AuthEvents /> */}
      Activity
    </div>
  );
};

export default ProfileActivity;
